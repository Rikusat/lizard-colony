# V5M モーション語彙拡張 要求仕様書 — ディレクター発行 (2026-07)

> Ric裁定「モーションが少ない」を受けた設計・実装アーク。
> リポジトリ `docs/` に配置し、HANDOFF §5s に **M1(候補提案)/M2(バッチ実装)** として登録すること。
> フェーズ1のプロンプトは発行済み(本書はその正本+以降の進行規定)。

---

## 0. 背景と目的

- コアコンセプト「トカゲを飼育し、眺める」に対し、現行モーション語彙は7種
  (歩行/揺れ/水飲み/暖取り/巣出入り/見上げ/避難)=**移動と生存のみ**。
- トカゲらしさ・生き物らしさ・関係性の語彙が不足しており、眺めの解像度が頭打ち。
- 目的: **「5分眺めて2〜3回の『お、』」の密度を、語彙の追加で引き上げる。**
  通知・演出で主張しない。気配だけ見せて説明しない(crank.mdの作法)。

## 1. フェーズ構成

### M1: 候補提案(発行済み・承認制)
- CCが新モーション候補15〜20種を提出。各候補に:
  (a) 一言説明
  (b) 実装方式 = 既存geometryの姿勢・回転・タイミングの組合せ/新規形状変形の要否
  (c) 発生条件・頻度の想定
  (d) 惑星別意匠へ発展し得る場合はその注記
- 候補の4分類(満遍なく):
  1. **トカゲ固有の行動**: 日光浴の伏せ・岩上静止・尾のゆらぎ・瞬膜/まばたき・舌出し・
     首かしげ・脱皮の気配 等
  2. **生き物の間(ま)**: 静止して周囲を見る・静→動の短距離走(トカゲ特有のリズム)・
     砂掘り・伸び 等
  3. **関係性**: 2匹が向き合う・若い個体の追従・ボス戦後に集まる 等
  4. **環境反応**: 惑星環境への反応・ルーレット球を目で追う・レア個体に他個体が寄る 等

### M2: 選定とバッチ実装(M1承認後)
- Ricが候補リストから採用を選定(全採用/一部/優先順位付けいずれも可)。
- R5-bで確立した進行型を踏襲: **基準1種目を先行実装**(最も「お、」を生む本命から)→
  Ric実機判定で頻度・振幅の勘所を確定→残りを3〜5種ずつのバッチ展開。
- 各バッチの検分セット: 実機での発生シーン連続スクショ(2〜3枚)+発生頻度の実測値+確認URL。
- **判定は必ずRicの実機目視**(モーションの「気持ちよさ」は静止画・数値で測れない)。

### M3: [A]②への接続(全バッチ完了後)
- 全モーションの頻度・振幅・有効/無効CFGキー一覧を[A]②眺めセッションへ登録。
- 既存モーション⑥5キー(poseBobPx/spotVisitChance等)と統合した「眺めの調整表」を1枚に。

## 2. 安全境界(全フェーズ不変)

- **魂の聖域との境界(本アークの最重要規律)**:
  - 可: 既存パーツ(胴/頭/尾/脚)の姿勢・回転・タイミング・組合せで作る動き。
  - 個別承認: パーツ形状の新規変形を要する動き(例: 日光浴で体を平たくする)。
    候補として提案は可。実装は候補ごとにRicの明示承認を得る。
  - 不可: トカゲの基本描画(魂)の恒常的変更。特性上乗せ・自切尾の既存例外は従来通り。
- 全モーションを個別CFG化: 頻度・振幅・有効/無効(将来の[A]②で調整する前提)。
- reduced-motion対応(動きは停止し静止で成立)。
- 決定論: 座標/id/時刻バケットのハッシュ由来。乱数不使用(スクショ比較・QA再現性)。
- 性能: _traitSigと同型の設計(該当個体のみ更新・静的1回描画+差分の方式を壊さない)。
  バッチごとに負荷増分を実測報告。
- 戦闘・経済・確率・純血・セーブは非接触。
- QAゲート3層+boot console0+同乗デプロイ禁止規律を全バッチで適用。

## 3. 判定基準(Ricの実機判定の観点・M2で使用)

1. **密度**: 5分の眺めで「お、」が増えたか(過密で騒がしくなっていないかも同時に)。
2. **トカゲらしさ**: 動きが爬虫類のリズム(静→動・じっとしている時間の長さ)に合うか。
   哺乳類的な忙しなさになっていないか。
3. **主張しない**: どのモーションも通知的・演出的に自己主張しないこと。
   気づいた人だけが得をする密度であること。
4. **群れの表情**: 個々でなく飼育槽全体として「生きている場所」に見えるか。

## 4. デプロイ方針

- R5-b同様、原則**全バッチ完了後の一括**(「トカゲが生き生きした」を1回の体験で届ける)。
- ただしバッチ数が多くなる場合(3バッチ超)は中間デプロイをRicと相談(本書を更新)。

## 5. 付記

- 本アークは[A]②眺めセッションの前提整備を兼ねる。M3完了までは②の既存5キー詰めを
  先行してもよい(領域が分離しているため衝突しない)。
- 関係性(分類3)・環境反応(分類4)の候補は、将来の観測施設拡張(HANDOFF残タスク)や
  コミュニティ機能と接続し得る。M1でその芽が見えた候補は注記しておくこと。
- 本書のフェーズを跨ぐ矛盾・不明点は、実装で吸収せずディレクターへ差し戻すこと。

## M2-EX: モーション第2波(2026-07-26・Ric裁定「誤差の範囲→明らかに生きている」・自律完遂)

Ric裁定を受けた拡張アーク。密度目標を「5分で2〜3回」→「5分で6〜10回の『お、』(同時多発で騒がしくしない)」へ引き上げ。
4パート構成をバッチ単位で自律実装(判定待ちに入らず・デプロイ禁止=M2と一括でRic実機判定後)。

- **パートA(可視性の再調律)**: 既存全モーションの頻度/振幅を新目標へ再調律。尾ゆらぎ/身震いは振幅up・
  キョロは頻度↓×`motLookDwell`新設で1回を長く印象的に。before/after頻度を実測報告。
- **パートB(見送り6種の解禁)**: ③まばたき/⑨伸び(C=形状変形・特則遵守)/⑪首かしげ/⑭勝利の集い/
  ⑯ルーレット目線/⑲波紋注目。⑨は⑥と同じ波及ゼロ証明(P1-P4)。
- **パートC(既存行動の多段化)**: 水飲み(頭下げ→見上げ+波紋)/暖取り(⑥接続+身じろぎ)/
  巣出入り(出巣で見回し・入巣前に振り返り)/見上げの揺らぎ。既存spot挙動への段階追加のみ。
- **パートD(新語彙第2波・CC起案→実装)**: D1あくび/D2頭プッシュアップ表示/D3尾フリック/D4群れの同期警戒/
  D5向き替え/D6まどろみ/D7漂う環境粒子(惑星別)/D8地面の味見。A/B方式8種を実装。

### M2-EX C方式(形状変形)の提案(個別承認待ち・未実装)
- **⑨伸び以外の追加C候補**: (a)片足上げの体温調整(脚ジオメトリ) (b)真のあくび=顎ジオメトリの開口
  (現行D1は口内の上乗せ描画で近似) (c)脱皮直後の全身ぶるっと(多パーツ協調)。いずれも魂の骨格に触れるため
  Ric個別承認を得てから実装する。

### デプロイ
- M2(13種)+ M2-EX(全パート)を一括で、Ric実機判定後にデプロイ。それまでローカル+push のみ。

## M2-EX2: C追加3種 + 巣口すり抜け(2026-07-26・自律完遂)

- **パートE(C方式追加3種)**: E1片足上げ体温調整(脚ジオメトリ変形・熱い惑星/暖取りで発生率up)/
  E2真のあくび=下顎の実開口(D1近似を置換=二重発火防止)/E3脱皮後の全身ぶるっと(多パーツ横揺れ・⑤直後のみ)。
  E1/E3は⑥⑨と同じ波及ゼロ証明(P1-P4)を各々通過。
- **パートF(巣口すり抜けの根治)**: 「巣へ向かう/出る」個体の相互回避を無効化=互いをすり抜けて通過。
  実測(40匹半数避難): すり抜けOFF+レーンなし=収容30秒超のデッドロック → ON=4.2秒で収容完了。
  レーン分散(nestEntryRadius)は視覚的重なり抑制の補完として残置(退役せず併存が最適=実測判断)。
  すり抜けは衝突/回避の一時無効化のみ・避難/戦闘の判定基準は非接触。
- **パートG(再調律の最終化)**: 視線系トリオ(look/headbob/turn)の突出を均す+dig抑制。notable約20/分・空間分散で騒がしくない。

### 全モーションCFG統合
- 全モーション(従来7+M2 11+M2-EX第2波14+M2-EX2のE3種)のon/off+頻度+振幅=**mot*/drink*/nest* 123キー**。
  [A]②眺めセッションで実機微調整する前提の「眺めの調整表」を構成。

### M2-EX2以降の残(Ric判定・スキップ)
- ⑱空への反応(動的環境演出の新設が前提=継続保留)。C方式のさらなる候補があれば都度起案→個別承認。

## M2-EX3: 設備連動モーション + 水波紋(2026-07-26・自律完遂)

新テーゼ「設備がトカゲの遊びを生む」=設備の成長段階(Phase8 tier)が上がると、その設備でしか見られない遊びが解禁。投資が眺めの豊かさに直結。

- **パートH(設備tier連動の遊び・発生tier閾値もCFG)**:
  - 水場(大湖tier4): 水遊び3変種=身を沈める/水を跳ねさせる(飛沫)/浅瀬で伏せて涼む。
  - UFO光(ビームtier3): 光の柱の中の浮遊=ゆっくり大きく上下(A方式=translate・魂形状不変で成立=C不要)。
  - 観測施設群(観測デッキtier3): 空をゆっくり見渡す横首振り。
  - すべて既存の施設tier読み取り(読み取り専用)+既存spot姿勢の拡張。新判定軸なし。
- **パートI(水場の足跡波紋)**: 水場spot/大湖上を歩く個体の接地点に波紋(歩行位相同期)。水tierで規模拡大。
  水の演出限定(地面には出さない)・決定論・装飾レイヤー(地形非接触)。

### H/Iの方式
- 全てA方式(placement translate)またはB/装飾overlay=魂ピクセル不変・キャッシュ非接触。**C方式は不要**(浮遊はtranslateで成立)。
