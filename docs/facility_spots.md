# facility_spots.md — 設備の「トカゲの居場所（スポット）」レジストリ

- 版: **0.3**（2026-07-21・モーション本体①〜⑤を実装＝spotFor割当/歩いて到達/姿勢の揺れ。⑥手触りCFGはRic実機）／0.2（2026-07-21・下ごしらえ＝座標コード化＋割当設計）／0.1（2026-07-20・Phase 8.5 新設）
- 位置づけ: 各設備が持つ**トカゲの居場所（スポット）**の座標・向き・想定姿勢を記録する（`GameExpansion_V5.md §8.5`）。
  **モーション（drink/bask/wade/lookout の実アニメーション）は未実装。** ここは「形状の設計時点で確保したスポット」の台帳＋割り当て設計であり、将来モーション実装時に参照する。
- **座標の単一の真実（v0.2でコード化）**: 下記のスポット座標は `Render.facilitySpots()`（`js/render.js`）が**現在の設備Lvから live に算出**する。本ドキュメントは意図・姿勢・確保方針を記し、**具体座標は関数が正**（二重管理を避ける）。関数は純粋な読み取り専用＝描画ループから呼ばれず挙動を変えない（不活性・モーションの接続点）。
- 座標系: 飼育槽バッファ 1280×720。設備の定位置は `FAC_POS`（render.js）。スポットは設備がその tier のとき有効。
- 向き: `→`(right, angle 0) / `←`(left, angle π)。姿勢=将来の motion 名の目安。
- 実装原則: スポットは**その設備の形状の中に物理的に確保**する（縁・ランプ下の空間・入口・浅瀬 等）。tier で規模が育つと、スポットも設備スケールに追従して移動しうる（座標は代表 tier のもの＋算出式）。

---

## スポット一覧（設備ごと）

各設備は「トカゲがそこで何をするか（action）」を持ち、その居場所を形状に確保する。

### 水場（water・P.water=(230,610)）
- **action**: 水を飲む（tier1-3）／水浴び・水遊び（tier4 大湖）。
- **spot（飲む・tier1-3）**: 池の**手前の縁**。座標 ≈ (P.water.x, P.water.y + ry\*0.85)（ry=waterTierInfo(lv).ry）。向き `→/←`（水に頭を向ける）。姿勢=「drink」（頭を下げて水面へ）。
- **spot（水浴び・tier4 大湖）**: **浅瀬**。代表座標 ≈ (300, 600)（岸に近い明るい浅瀬）。向き=水（左下）へ。姿勢=「wade/splash」（浅瀬に半身を浸す・尾で水を跳ねる）。浅瀬帯は岸辺の弧に沿って ~ (250〜360, 585〜620)。
- 形状の確保: tier1-3=手前の縁を平らに開ける。tier4=岸に接する**浅瀬の帯**を明るく確保（陸から歩いて入れる）。

### 保温設備（heat・P.light=(1010,322)）※§8.8 UFO転送ビーム / §8.12 で旧・展望台位置(右=ボス側)へ移動
- **action**: 暖をとってくつろぐ（バスキング）。**tier3/4 は群れが入れる広い光面**。
- **spot（バスキング・tier1-2）**: **ランプ直下/温室内の温床**。座標 ≈ (P.light.x, P.light.y + 44)。向き=任意（光源=上）。姿勢=「bask」。
- **spot（バスキング面・tier3-4 空中ライト）**: **地上に降りるビームの広い光円**（= heatTierInfo(lv).beamR 半径の面。群れが集まれる）。中心 ≈ (P.light.x, P.light.y)。姿勢=「bask」（面のどこでも伏せられる）。
- 形状の確保: tier1-2=温床の平地。tier3-4=空中エミッターからのビーム着地点に**半径 beamR の明るい光面**を確保（放射グラデ）。tier4 は光柱にシルエットが昇る転送演出（装飾・実個体とは別）。

### 展望台（observatory・FAC_POS.observatory=(640,300)）※§8.9 観測施設群 / §8.12 で中央へ配置し巨大化(ランドマーク)
- **action**: 観測する・空を見上げる（群れで集まる）／塔上で見張る。
- **spot（見上げ・群れの面）**: **手前の観測広場（淡い舗装の面）**。中心 ≈ (cx, gy+16)、半径 ≈ (deck幅)×0.66。向き=上（空）。姿勢=「lookup/bask」（面のどこでも見上げられる＝収容力）。
- **spot（見張り・tier3）**: **観測塔の塔上バルコニー**。座標 ≈ (cx + hw×0.72, gy − h×0.92)（手すり付き）。向き=遠く/空。姿勢=「lookout」。※高所のため代表は1匹。
- 形状の確保: 施設群の手前に**広い観測広場の面**を開ける（構造物は奥の板張り土台に乗せ、群れは手前の広場に立つ）。塔上に平らなバルコニーを確保。

### ~~餌場~~（撤廃・§8.12）
- **撤廃済み**（v12→v13）。効果（コオロギ湧き/自動給餌/食料供給）は**すみか（巣）Lv**へ統合（天井維持：巣Lv8≈旧餌場Lv10）。造形（放射状トラフ/培養槽/分配ガントリー）は将来すみか等へ流用可。投資済みLvはGold全額払戻。

### ~~繁殖施設~~（撤廃・§8.12）
- **撤廃済み**（v12→v13）。効果（繁殖CD/上位種変異/モーフ変異/伝説/Lv3予兆/Lv5予約）は**すみか（巣）Lv**へ統合（天井維持：巣Lv8≈旧繁殖Lv15）。投資済みLvはGold全額払戻。営巣＝すみかへ一本化。

### すみか（巣穴・FAC_POS.burrow=(185,322)）※§8.12 左へ移動＋給餌/繁殖効果を統合／§8.16 多入口の集合住居へ再設計（コロニーの中核）
- **action**: 複数の入口から出入りする・入口で日向ぼっこ。ベビーの避難先・強個体の籠り先。
- **spot（出入りの動線）**: **複数の入口**（`Render.burrowEntrances(nest.lv)`＝住居Lvで2/3/4個）。各個体は `Game.nestEntryFor(lz)`（id で安定割当）で**別々の入口**へ歩く＝動線が詰まらない（§8.14 と連動）。入口 world 座標は中心(185,322)から `gap` 間隔で横に展開。
- **spot（日向ぼっこ）**: 各入口の手前の平地。向き=穴（上）or 外（下）。姿勢=「enter/emerge」or「bask」。
- 形状の確保: 大きな盛り土に入口を横に複数確保（描画 `drawBurrow` と入口座標 `burrowEntrances` を共有＝二重管理なし）。当たり判定 hitR は入口の展開幅に追従（Lv1:84 → Lv8:193）。

### ~~シェルター~~（撤廃・§8.10）
- **撤廃済み**（v11→v12）。ベビー保護は「巣の基本仕様」として標準化（`injureLizards` で無条件にベビー非対象）。ガード回避は廃止。投資済みLvはGold全額払戻。定位置(旧170,320)は保温設備の移動先に転用。

### 防衛設備（§8.17 tier化・右側=ボスを迎え撃つ構え）
- **フェンス**（FAC_POS.fenceX=1218・右端の防柵）: 構造物（居場所なし）。tierで木柵→補強柵→丸太の防柵（杭を右=ボス側へ・横桟増・土塁+旗）。当たり判定=縦帯。
- **監視塔**（FAC_POS.watchtower=(1150,254)・右上の物見櫓）: **spot（見張り）＝塔上の見張り台**（座標 ≈ (x, gy − watchtowerTierInfo.h)、手すり付き）。向き=右（ボスの接近）。姿勢=「lookout」。当たり判定は塔高に追従（頂部中心）。※高所のため代表1匹。
- **罠設備**（FAC_POS.trap=(1128,470)・ボス進入路 y=470）: 構造物（居場所なし・むしろ敵を捕らえる面）。tierで杭列→落とし穴+網→焼却罠（篝火）。牙は右（ボス側）へ。ルーレット盤/右端UIを避けボスレーンに配置。当たり判定=杭列幅に追従。

---

---

## スポット座標レジストリ（コード化・v0.2 で確定）

`Render.facilitySpots()` が返す各スポットの構造（live world 座標。既存 `waterTierInfo`/`heatTierInfo`/`observatoryTierInfo`/`burrowTierInfo`/`watchtowerTierInfo`＋`FAC_POS`＋`burrowEntrances` から重複なく算出）:

```
{ id, facility, action, posture, capacity, facing, center:{x,y}, radius, tier }
```
- `posture`: 将来の motion 名の目安（`drink`/`bask`/`wade`/`lookup`/`lookout`/`emerge`）。
- `capacity`: そのスポットに同時に居られる目安人数（面が広いほど大。例: 群れバスキング面=8、塔上見張り=1）。
- `facing`: `right`/`left`/`both`/`up`（頭の向きの目安）。
- `radius`: スポットの面の半径（点スポットは小さい値）。tier で規模が育つと `center`/`radius` が追従。

**現在の出力（実測・2026-07-21）**:
- 新規ゲーム（設備0・巣Lv1）: `burrow-entry-0/1`（入口2）。※水場は建設時に出現。
- 全設備MAX相当（water20/heat20/obs10/watch10/巣Lv8）: `water-drink`(縁) `water-wade`(浅瀬) `heat-bask`(群れ光面 r190) `obs-lookup`(観測広場 r112) `obs-lookout`(塔上1匹) `burrow-entry-0..3`(入口4) `watch-lookout`(塔上1匹) の**10スポット**。全座標が盤内(1280×720)・tier追従を検証済み。
- フェンス/罠は構造物＝居場所スポットなし（敵を捕らえる面）。

## 割り当て機構の設計（未実装・モーションの土台）

「どの個体がどのスポットに居るか」を決める仕組み。**まだ実装しない**（動き＝手触りは Ric 実機判定が要るため）。設計方針のみ確定:

- **安定割当（`nestEntryFor` の一般化）**: 現状 `Game.nestEntryFor(lz)` が「個体id → 巣の入口」を id ハッシュで安定割当している（§8.14/§8.16）。これを**全スポットへ一般化**する: `spotFor(lz)` が id ハッシュ＋スポットの `capacity` を見て、空いているスポットへ決定論的に割り当てる。**同じ個体は毎フレーム同じスポット**（ちらつき防止・決定論=Fable2）。
- **容量（capacity）で群れ表現**: 広い面（群れバスキング/観測広場）は多く、点スポット（塔上見張り）は1匹。§8.13 の「同種2体制限」表示ルールと矛盾しない範囲で、可視個体の一部を"居場所へ向かうidle行動"に割り当てる。
- **空き（誰も居ない）を許容**: 全スポットが埋まる必要はない。可視個体数が少なければ空きスポットが出るのが自然（`capacity` は上限であって定員ではない）。
- **状態フックの契約（実装時に用意する空フック）**: 個体に `lz.spot`（割当先スポットid or null）と `lz.spotPhase`（姿勢アニメの位相 0..1）を持たせる想定。**現在は未定義＝全個体 spot なし（=従来どおり自由徘徊）**。motion 実装時にここへ書き込む。魂（`_paintLizardBody`）は不変を保ち、姿勢は `_paintLizardState` 相当の状態レイヤーで加える（スプライトキャッシュ sig に posture を足す＝§8.15 整合）。
- **表現層の規律（Fable1）**: スポットへ歩く/留まる/姿勢をとる、はルール層のイベント購読ではなく**表現層の idle 行動**（生産/戦闘/繁殖の数値には一切影響しない・純装飾）。`facilitySpots()` は座標を供給するのみ。

## モーション実装（①〜⑤ 実装済・2026-07-21／⑥ 手触りは Ric 実機）

**①〜⑤を実装した（C1〜C4）。⑥（手触り・振幅・"間"のCFG調整）だけ Ric の実機判定を残す。**

1. ✅ **`Game.spotFor(lz)`**（`nestEntryFor` の一般化）＝`facilitySpots()` の capacity に比例させ id ハッシュで**決定論的に安定割当**（Math.random不使用・同個体は同スポット＝ちらつき防止）。純装飾＝数値非干渉。設備スポットが無ければ null（従来どおり自由徘徊）。
2. ✅ **`lz.spot`/`lz._toSpot`/`lz._spotFacing`/`lz._spotPosture` フック**（既定 undefined＝非干渉）。徘徊の再設定時に `CFG.spotVisitChance` で最寄りではなく**割当スポット**へ向かう（面の中は id で決定論オフセット＝固まらない）。戦闘・帰巣・負傷・ロード（`settleDisplay`）で解除＝stale姿勢なし。
3. ✅ **歩く動線**＝既存 `moveLizards` の徘徊移動を流用（`lz.tx/ty` をスポットへ向ける）＝**ワープ禁止（§8.14）**。到達（`dist<=4`）で `lz.spot` 確定＋`spot.facing` へ向き付け。
4. ✅ **姿勢の揺れ `Render._poseBob(lz)`**（drink=水面へ頭を沈める上下/bask=呼吸/wade=尾で水を跳ねる左右/lookup・lookout=見上げて浮く/emerge=入口の日向ぼっこ）。**魂ピクセル不変を厳守＝配置トランスフォーム（整数 px の bob）のみ**（doc案の「キャッシュ sig に posture 追加＝焼き込み」ではなく、**より強い保証**＝魂の焼きスプライトは一切変えずキャッシュ無効化もなし・crisp維持）。`drawLizard` で body+state を一体に translate。`CFG.poseBobPx/poseBobSpeed`。
5. ✅ **`reduced-motion` で姿勢停止**（`_poseBob` が null＝静的スポット滞在は残る＝暗転しない）。※本作の reduced-motion は `Motion.reduced`（`fx0` の別トグルは無し＝これで担保）。
6. ⏳ **Ric 実機判定（⑥・未確定）**: 手触り・"間"・振幅（`CFG.poseBobPx/poseBobSpeed`）・どのスポットにどれだけ集まると気持ちよいか（`CFG.spotVisitChance/spotDwellMin/Max`・`facilitySpots().capacity`）。**強いポーズ（実際の頭下げ・伏せの体形変化）は魂geometryに触れるため、許容するか＝Ric判断で別途**（現状は魂不変の整数bobで"生きている"を表現）。

- 検証: 恒久 `tests/motion_regression.js` **22 PASS**（C1割当:決定論/capacity比例/状態非干渉/null・C2配線:到達/ワープなし/戦闘・帰巣・負傷・ロードで解除/数値非干渉・C3姿勢:整数bob/null条件/決定論）。実canvas描画6姿勢0err・純血59・phase6 86・boot console0。fps影響=軽微（16匹draw換算 pose ON 5580 / OFF 6027fps＝両者60fps遥か上）。

---

## 点検メモ（8.5 の観点で既存6設備を確認・随時更新）
- 優先3設備（大湖/保温/展望台）は本スプリントで**スポットを形状に確保**して再設計。**完了**：大湖=浅瀬帯 / 保温=空中ライトの光面（§8.8） / 展望台=観測広場の面＋塔上バルコニー（§8.9・観測施設群化）。
- 残り（餌場/繁殖/すみか/シェルター）は既存形状にスポット相当の平地があるかを点検し、無ければ後続で確保。現時点の見立ては上記「形状の確保」に記載。
