// ============================================================
// 巣ビジュアル数値ゲートの閾値 — 単一の真実(V6-P2-2 B0/B1 仮設・2026-08-11)
//   参照 = image/nest_image2.png のコンテンツ領域 (212,57,1460×884)。ref値は 2026-08-11 CDP実測。
//   座標系 = コンテンツ領域ローカル(1460×884)。使用者 = test-nest-cut.html(部分ゲート)。
//   運用 = 初期値は緩め→バッチ完了ごとに実測して締める。**緩和には理由記録+Ric承認**(§5x-OPS ⑮)。
//   B5で全画面ゲート(CDPスクショ+node比較)へ拡張する。
// ============================================================
// ★B4改訂(2026-08-11): 再現側のサンプル幾何は**すべてコア相対**(B2教訓の徹底=ステージ幅がパネル有無で
//   変わっても壊れない)。ref値は参照の絶対座標(コア618,362基準)で実測済み=不変。
//   rect の再現側 = [コア相対dx, dy, w, h] / band の再現側 = 実コア中心+同半径。
const NEST_VIS_THRESHOLDS = {
  // 背景サンプル(ノード・糸の掛からない素の地)。参照絶対(928,643)=コア相対(+310,+281)
  bg: { rectRel: [310, 281, 130, 130], ref: { avgL: 24.9 }, avgLTol: 10, warmMax: 2 },
  // コア矩形(編み枝の鉢+卵+光暈)。参照絶対(478,233)=コア相対(-140,-129)
  core: {
    rectRel: [-140, -129, 300, 280],
    ref: { avgL: 43.3, warm: 14.3, cen: [144, 160], bright: 1.03 },
    avgLRel: 0.20,          // 平均輝度 ±20%相対
    warmMin: 7, warmMax: 29, // 暖色画素率(r>g>b かつ L>60)の許容帯(参照の約0.5〜2倍)
    cenDist: 15,             // 高輝度重心(L>170)の距離px
    brightMin: 0.5, brightMax: 2.1, // 高輝度画素率の参照比
  },
  // B2 糸+輝点(2026-08-11実測)。amber判定 = r>96 && g>56 && b<64 && r>g && g>b*1.6 && L>50(参照と再現で同一パイプライン)
  // ★指標の再設計(緩和ではなく適切化・2026-08-11・要Ric承認):
  //   初版は「参照の糸が通る矩形2点」を測ったが、実UI(81ノード・リング半径75〜449)とモック(約14ノード・
  //   リング221/380)の**トポロジ差**により、矩形一致は角度の偶然を測ってしまう(帯1が通ったのも偶然・帯2は不成立)。
  //   → **環状帯(annulus)統計**へ変更: コア中心(618,362)から半径110〜200の帯=参照で糸だけが通る領域
  //   (メダリオン環221より内側・鉢135より外側)。角度非依存=幾何差に頑健で「糸の質感がその帯に在るか」を測る。
  threads: {
    band: { r0: 110, r1: 200, ref: { avgL: 38.8, amber: 1.77, warm: 6.8 } }, // 中心=各側の実コア(参照=618,362)
    bandAvgLTol: 10,                  // 帯の平均輝度 ±10L
    bandAmberMin: 0.9, bandAmberMax: 3.6, // 帯のamber率(参照1.77の0.5〜2倍)
    bandWarmMax: 14,                  // 帯の暖色率上限(参照6.8の約2倍)
    // 全域の琥珀予算: 参照コンテンツ全域の amber=2.55%(ノードリム/ラベル込み)。
    // 糸だけ(B2時点=ノード未実装)でこの予算を超えないこと=Ric指示「糸の占有が琥珀アクセントに収まる」。
    // 下限0.5%=糸が実在すること。B5(ノード込みの全画面ゲート)で参照±へ締め直す。
    contentAmberRef: 2.55, contentAmberMin: 0.5, contentAmberMax: 3.06, // 上限=参照×1.2
  },
  // B3 ノード意匠(2026-08-11)。計測=実寸シュートゲート(tools/qa-nest-shot.mjs が test-nest-b3.html=
  //   **実コード経路そのまま(buildNest+layoutStage・DOM+canvas)**を1460×884でスクショしnode側で解析)。
  //   帯=参照のメダリオン環(半径221)を含む環状帯200〜330(B2の教訓=角度非依存・トポロジ差に頑健)。
  //   実UIは同帯に3環48ノード(モックは約8個+ラベル)=数の差は再現対象外(Ric裁定)・意匠の統計一致を測る。
  nodes: {
    band: { r0: 200, r1: 330 },                     // 中心=各側の実コア
    ref: { avgL: 45.8, amber: 5.11, warm: 11.2 },   // 参照同帯の実測
    avgLTol: 10, amberMin: 2.5, amberMax: 10.2, warmMax: 22,
  },
  // v2-V1 素材コアの自己アンカー型ゲート(2026-08-11): 素材(nest-core.png)そのものが正解。
  //   高α(a≥250)画素を素材→スクショへ写像し平均|ΔL|で**無改変+正配置**を検査(モックとの輝度一致は
  //   素材要素では測る意味を失うため対象外へ=B0/B1/B2はコード描画=フォールバック経路の回帰として存続)。
  asset: { maxMeanDL: 3, minSamples: 3000, cenDist: 6 },
  // B4 右パネル列(2026-08-11実測・参照絶対rect=(1078,15,380,830))。再現側=実DOMの#nest-side bbox
  //   (qa-nest-shotが--evalで取得)。幅が違っても密度(%)比較=スケール頑健。hi=L>140(明るい文字/数値の存在)。
  panel: {
    ref: { avgL: 33.0, amber: 0.60, warm: 3.8, hi: 2.19 },
    avgLTol: 10, amberMax: 1.5, warmMax: 8, hiMin: 1.0, hiMax: 4.4,
  },
};
// node側ツール(tools/qa-nest-shot.mjs)からの読込用(ブラウザでは無視される)
if (typeof module !== "undefined" && module.exports) module.exports = NEST_VIS_THRESHOLDS;
