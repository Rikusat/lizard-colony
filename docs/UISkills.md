# UISkills.md — Lizard Colony UIデザインシステム

- 版: 1.0（2026-07-11）
- 目的: **Claude CodeがUIに触れる前に必ず読む単一の参照ファイル。** ここに書かれたトークン・コンポーネント仕様・モーション規則・アクセシビリティ規則に**すべてのUI実装を従わせる**。ここに無い独自スタイルを散発的に足さない。
- 本ファイルは**仕様（値の定義）**であり、コードそのものではない。値はCSSカスタムプロパティ等へ落とし込む前提の初期値。実名・単位は既存規約に合わせてよいが、**役割と関係性は保つ**こと。

---

## 0. デザイン言語「Neo-Terrarium（ネオテラリウム）」

> **魂はレトロ。額縁はモダン。**

このゲームは「飼育ケース（テラリウム）を覗く」体験が原点。ドットの生き物・琥珀色の土・素朴な温かさが**魂**であり、これは絶対に失わない。近代化するのは生き物ではなく、その**周囲の枠（パネル・ボタン・遷移・フィードバック）**。

### 5つの柱（全UI実装の判断基準）

1. **魂を残し、皮を磨く** — ドット生物・大地のフィールド・琥珀パレットは維持。モダン化は"額縁"に限定。
2. **平常は静か、ピークは大胆** — 待機画面は可読で落ち着く（レトロ世代の安心）。ボス・巣解放・惑星移動・孵化などのピークは映画的に沸かせる（若年層の高揚）。**この落差が戦略**。
3. **すべてにジュースを** — タップは弾み、数字は回り、報酬は弾ける。ばね（spring）ベースの微反応で"生きている"手触りを出す。ただし常時多用ではなく、行動に対する即時の1発で。
4. **デスクトップ最優先** — PCブラウザ前提。マウス操作（ホバー状態を積極活用）とキーボード操作を軸に、広い画面を活かした情報設計にする。
5. **迷わせない** — 情報階層を明確に。1画面の主役は1つ。装飾が可読性を殺さない。

### 判断に迷ったら
- 「これは生き物（魂）を汚していないか？」→ 生物・フィールドの改変は原則しない。
- 「これは平常画面を賑やかにしすぎていないか？」→ 賑やかさはピークへ寄せる。
- 「新しいスタイルを場当たりで足していないか？」→ 本ファイルのトークン/コンポーネントに無いなら、まずここへ追記してから使う。
- 「絵文字を使っていないか？」→ **絵文字は全面禁止**。すべてのアイコンは自作SVG/スプライトで用意する（§9）。

---

## 1. カラートークン

### 1.1 ベース（大地・飼育ケースの土壌）※魂の色

| トークン | 用途 | 参考値 |
|---|---|---|
| `--ink-900` | 最暗部・背景の底 | #1A120B |
| `--ink-800` | パネル背景 | #241812 |
| `--ink-700` | パネル一段上・境界 | #33261C |
| `--ink-600` | 区切り線・非活性 | #4A3728 |
| `--sand-300` | フィールドの土（明） | #B8895A |
| `--sand-200` | テキスト補助・淡 | #D8C3A5 |
| `--paper-100` | 本文テキスト（土の上の白） | #F3E9D8 |

### 1.2 ブランド／プライマリ（琥珀＝資源・格式）

| トークン | 用途 | 参考値 |
|---|---|---|
| `--amber-500` | プライマリ・Gold・見出しアクセント | #C9A227 |
| `--amber-400` | ホバー・発光 | #E4BC3A |
| `--amber-600` | 押下・影側 | #9A7A18 |
| `--amber-glow` | 発光ハロー（rgba） | rgba(228,188,58,.45) |
| `--amber-glow-soft` | ホバー等の弱い発光（Phase 2追記） | rgba(228,188,58,.20) |

### 1.3 セカンダリ（トカゲの緑＝生命・行動）

> ロゴのトカゲの緑を"生命と行動"の色に昇格。CTA・成長・繁殖・ポジティブに使う。モダンな鮮度をこの1色で担保。

| トークン | 用途 | 参考値 |
|---|---|---|
| `--life-500` | 主CTA・繁殖・成長 | #5FBF6A |
| `--life-400` | ホバー・発光 | #7BD986 |
| `--life-600` | 押下 | #3E9E49 |
| `--life-glow` | 発光ハロー | rgba(123,217,134,.40) |
| `--on-life` | 生命の緑面(CTA)上のテキスト（Phase 2追記） | #0F2E14 |

### 1.4 セマンティック（意味の色）

| トークン | 意味 | 参考値 |
|---|---|---|
| `--boss-500` | ボス・危機・侵食（ember red） | #E0533B |
| `--boss-400` | 暗背景上のボス系テキスト（Phase 7追記・AA実測 ink-700上5.6:1） | #F28066 |
| `--boss-600` | ボスの押下・影側（Phase 1追記） | #8F2F1C |
| `--info-500` | 情報・水・冷（cyan） | #46B7C9 |
| `--gem-500` | ダイヤ | #4FC3E8 |
| `--amethyst-500` | アメジスト（最上位鉱石） | #9B6BD6 |
| `--warn-500` | 警告 | #E8A13A |
| `--on-amber` | 琥珀面(primary/金バッジ)上のテキスト（Phase 1追記） | #241505 |

### 1.5 惑星アクセント（Phase 8で適用・惑星ごとに1色）

各惑星は環境色をアクセントに持つ。ヘッダーの縁・惑星マップのリング・その惑星専用UIの差し色に使う。**ベース土壌は共通のまま、差し色だけ変える**（世界観の切り替えを軽く演出）。

| 惑星 | アクセント | 参考値 |
|---|---|---|
| 砂漠 | 砂金 | #D9A441 |
| 草原 | 若草 | #7FB856 |
| 密林 | 翡翠 | #2FA98A |
| 古代古墳(旧・湿地) | 苔緑 | #6E8C4A（2026-07-17改名・墳丘の苔として色は据え置き） |
| 火山 | 熔岩 | #E0533B |
| 氷の前線(旧・雪原) | 氷水 | #7FC7DE（2026-07-17改名・色は据え置き） |
| 洞窟 | 燐光 | #6FB8A0 |
| 廃原子炉 | チェレンコフ光 | #6FB8A0（2026-07-17・ID9世界観差し替え。洞窟の燐光を冷却光として引き継ぎ） |
| 水中都市 | 深海光 | #5FA8C9（2026-07-17・ID7世界観差し替え・静寂の水色） |
| 遺跡 | 黄昏金 | #C9A227 |
| 摩天楼スラム | ネオン | #D957B0（2026-07-17追記・ID2世界観差し替え） |

### 1.6 使用規則
- **1画面に鮮色（life/boss/gem等）を氾濫させない**。鮮色はCTAと報酬とピークに限定。平常面は土壌＋琥珀が支配。
- **色だけで意味を伝えない**（アイコン・形・ラベルを併用。色覚多様性対応）。

---

## 2. タイポグラフィ

**モダンとレトロの二刀流**でフォントを使い分ける（Neo-Terrariumの体現）。

| 役割 | 方針 | 推奨（日本語対応・無償） |
|---|---|---|
| UI本文・データ | 現代的で可読なゴシック | Zen Kaku Gothic New / M PLUS 1p |
| 見出し・親しみ | 丸みのある親近感 | M PLUS Rounded 1c |
| **レトロ差し色** | ドット/スタンプ的な懐かしさを**限定使用** | DotGothic16（STAGE名・実績・称号・"BOSS"等の"判子"的ラベルのみ） |
| 数値カウンター | **等幅数字（tabular-nums）必須** | 上記＋`font-variant-numeric: tabular-nums` |

### 規則
- **レトロフォントは全面に使わない**。可読性を守るため、UI本文は必ずモダンゴシック。ドットフォントは"懐かしさのスパイス"として名詞ラベルに点在させるだけ。
- 大きな数値（Gold・トカゲ数等）は等幅数字で、桁揺れを防ぐ。
- サイズは`clamp()`で流体化（モバイル〜デスクトップで自動調整）。基準: 本文16px / キャプション13px / 見出し20〜28px / ヒーロー数値32〜48px。
- 行間は日本語で1.6前後。字間は見出しのみ僅かに詰める。
- **字サイズの一律倍率(2026-07-17 V5 Phase2)**: 全UIの font-size は `calc(Npx * var(--fs-scale, 1))` 形式とし、`--fs-scale`(tokens.css・現在1.1)の1値で一律調整する。px直書きの font-size を新規に足さない(必ずこの形式で)。canvas内の描画テキスト(ctx.font)は対象外(別途)。

---

## 3. 空間・角丸・段差

### 3.1 スペーシング（4pxグリッド）
`--space-1:4 / -2:8 / -3:12 / -4:16 / -5:24 / -6:32 / -8:48`。**余白はケチらない**——現代UIの余裕は余白から。パネル内padは最低16px。

### 3.2 角丸
| トークン | 値 | 用途 |
|---|---|---|
| `--radius-sm` | 8px | バッジ・小要素 |
| `--radius-md` | 14px | ボタン・カード |
| `--radius-lg` | 20px | パネル・モーダル |
| `--radius-pill` | 999px | 資源ピル・タブ |

### 3.3 段差（Elevation）— フラットから"層"へ
現状のフラットなパネルを、**やわらかい影＋内側のふちどり光**で層状にする。重すぎるドロップシャドウは禁止（レトロの素朴さを壊す）。

| トークン | 用途 | 参考 |
|---|---|---|
| `--elev-1` | パネル | 影 y2 blur8 rgba(0,0,0,.25) ＋ 内側1pxハイライト |
| `--elev-2` | 浮遊カード・ボタン | 影 y4 blur16 rgba(0,0,0,.30) |
| `--elev-3` | モーダル・ヒーロー | 影 y12 blur40 rgba(0,0,0,.45) |
| `--glass` | オーバーレイ背景 | 半透明ink＋`backdrop-filter: blur(12px)`（対応外はソリッドfallback） |

### 3.4 移行期の中継トークン（Phase 1追記・暫定）

- **レガシー橋渡し**: 旧`style.css`の変数名（`--gold`/`--sub`/`--card`等）は`tokens.css`内でトークンへのエイリアスとして存続させる（JSインラインスタイルの参照を壊さないため）。Phase 2〜4の画面刷新で参照側を直接トークンへ置換し、段階的に撤去する。`--card-hi`のみ`--ink-600`と近接し境界との区別が消えるため現値(#4d3a20)を暫定維持。
- **サーフェス中継**: 既存の繰り返しグラデーション面は`--grad-btn`/`--grad-card`/`--grad-row`等の中継トークンに保存（見た目不変）。Phase 2のコンポーネント刷新で§5仕様のサーフェスへ正式定義し直す。

---

## 4. モーション

**ばね（spring）を基調に。線形は使わない。** モーションはNeo-Terrariumの"生きている"手触りの核。

### 4.1 デュレーション
| トークン | 値 | 用途 |
|---|---|---|
| `--dur-fast` | 120ms | タップ反応・ホバー |
| `--dur-base` | 200ms | 状態遷移・表示切替 |
| `--dur-slow` | 320ms | パネル/モーダル出入り |
| `--dur-cinematic` | 600–1200ms | ヒーロー演出（ボス・巣解放・惑星移動） |

### 4.2 イージング
| トークン | 曲線 | 用途 |
|---|---|---|
| `--ease-pop` | cubic-bezier(.34,1.56,.64,1) | 弾む出現・タップ（オーバーシュート） |
| `--ease-out` | cubic-bezier(.16,1,.3,1) | 滑らかな遷移 |
| `--ease-in-out` | cubic-bezier(.65,0,.35,1) | 往復・ループ |

### 4.3 定番パターン
- **数値カウントアップ**: 資源増加は即置換せず、短時間で回して着地（`--dur-base`）。桁は等幅。
- **タップ弾み**: 押下scale .96 → 離しscale 1.0を`--ease-pop`で。
- **報酬バースト**: 獲得時に対象へ向けパーティクル数粒＋発光ハロー。
- **入場choreography**: リスト/カードは1要素ずつ40–60ms差でstagger表示。
- **ヒーロー**: 暗転→主役ズームイン→発光→情報ワイプ（§コンポーネントのヒーロー）。

### 4.4 reduced-motion（必須）
`prefers-reduced-motion: reduce`時は、オーバーシュート・パーティクル・カメラ移動を止め、**フェード＋即時値反映**に置換。演出でゲームを止めない。

---

## 5. コンポーネント仕様

各コンポーネントは「構造 / 状態 / モーション」で定義。既存の同種要素はこれに寄せて統一する。

### 5.1 資源ピル（ヘッダー常設: Gold/トカゲ/ダイヤ/鉱石）
- 構造: `[アイコン][数値(等幅)][増分レート(小)]`をpill形状（`--radius-pill`）に。背景`--ink-700`＋内側ハイライト。
- 状態: 増加時に数値カウントアップ＋アイコンが1回弾む＋琥珀の発光が一瞬。
- モーション: 増加`--ease-pop`。減少は控えめ（色を一瞬`--warn`寄せ）。

### 5.2 アクションボタン — 操作卓モジュール（Brushup V2で確定・案B+立体の手触り）
- **造形**: 真鍮の制御盤モジュール（`--grad-console`・radius 6px・控えめなコーナーねじ2点）。上面ハイライト＋押下で沈む立体（案Aの手触りを借用）で無機質になりすぎない。
- **種別の見分け＝左端インジケータバー＋計器窓（色だけに依存しない構造）**: 主行動=`--life`・ショップ=`--amber`・危機=`--boss`・画面遷移=`--ink-600`（落ち着き）。アイコンは暗い機器窓（inset）に沈める。
- **計器窓（コストバッジ）**: `--lcd-bg/--lcd-text/--lcd-border`＋DotGothic16（レトロの魂を機能表示に）。
- **トグル＝ランプ＋ロッカースイッチ**（一目でスイッチと分かる形状・role=switch）。
- 状態: 通常/ホバー(発光)/押下(沈む+scale.97)/無効50%/長押しプログレスリング（継承）。給餌クランク（§5.10）と同じ「飼育管理装置」の世界観で統一。

### 5.3 カード／パネル
- `--radius-lg`・`--elev-1`・内側1pxハイライト。見出しは小さめ＋区切り。
- 情報過多を避け、1カード1主題。

### 5.4 モーダル
- 背景`--glass`（blur）で後景を沈める。本体`--elev-3`。出入りは`--dur-slow`＋`--ease-out`（下からせり上がり or 中央スケール）。
- 中央スケールインを基本形に。オーバーレイは画面中央へ集約し、後景は`--glass`で沈める。

### 5.5 トースト／通知
- **位置(3.11改訂)**: 飼育槽(#frame)内の**上部中央**に配置し、**スマホ通知のように上から下へスライドイン**、数秒で上へ退場。左上の惑星名バッジ・右上の襲撃予告HUD・右下のクランクと**重ならない中央上**に置く(max-width 56%で中央寄せ)。
- **半透明・控えめ**: 背景は半透明(≈.82)＋blurで、生き物の描画を隠さない(「平常は静か」)。`pointer-events:none`でフィールドのクリックを妨げない。
- **上限4件・古いものから消す**: オート給餌中の孵化/成長通知の大量発生でも画面を埋めない。
- 種別色: 成功`--life` / 危機`--boss` / 情報`--info` / 報酬`--amber`(既定)を**左端インジケータバー**で示す。アイコン併用。reduced-motionでアニメ停止。

### 5.6 タブバー（設備の通常/防衛、図鑑の惑星別 等）
- pill型セグメント。選択タブは塗り＋僅かな浮き。切替は下線 or 背景がスライド移動（`--ease-out`）。

### 5.7 通知バッジ（惑星マップ/タブ）
- 小円に数 or ドット。種別色。出現時に1回パルス。**常時点滅させない**（不安を煽らない）。

### 5.9 テラリウムのガラス面（メイン画面の額縁・Phase 3追記）
- 中央フィールドを「飼育ケースのガラス越しに覗く」額縁として縁取る。**中の生物・大地の描画(Canvas)には一切干渉しない**(pointer-events: none のCSSオーバーレイ)。
- 構成: 内側1pxのふちどり光＋上端ハイライト＋ごく淡いヴィネット＋最小限の斜め映り込み。
- 強度は2トークンで一元制御: `--terra-glass`(ふちどり光・ヴィネットの総量 0〜1) / `--terra-sheen`(映り込み 0〜1)。初期値は弱め(未決定2)、ディレクター確認で調整。

### 5.10 給餌クランク（v2.1集約 → V5.2 §2 引き算UX改訂）
- 構造: `[大クランク][レート(タップ循環)][オート(後光)][安全装置(後光)]`のコンパクトな塊。**囲み枠・内蔵ランプ・余分な文字は撤廃**し、状態は「ボタン全体の後光」で示す（引き算のUX）。在庫はトップバー＋右パネルに表示（盤面には出さない）。
- **後光による状態表示（V5.2・アンカー改訂）**: **オートON＝ボタン全体が緑の後光**（直感アンカー「緑＝オート」を、内蔵ランプでなく全体発光で表現）。**安全装置ON＝ボタン全体が琥珀の後光**（緑と混同させない）。枠なし・ランプなし。CSSは `#feeder-dial#feeder-dial .fd-sw.fd-glow`（詳細度2,2,0）で全スキンの `.fd-sw` 背景（1,2,0）に順序非依存で勝ち、枠を確実に消す。
- **操作**: タップ=1掴み / 長押し=連続給餌（＝オート高と同速=CFG.dialRates[2]） / **レート=ボタン全体タップで 低→中→高 を循環**（狭い位置クリックを強いない・広い当たり） / オート=現レートで自動。
- **安全装置トグル**: オートと同サイズ。ON=自動停止（琥珀後光）/ OFF=Gold換算補充。**OFF＋オート中に在庫<`CFG.cricketLowWarn`で赤の警告ランプが点滅**（在庫僅少の予告・reduced-motionで点滅停止）。
- **ドラッグ移動**: 群の左上グリップ（点3つ）で、ボタン群を1つの塊として移動できる。位置は `localStorage.feederPos` に保存（次回復元・親#center内にクランプ）。
- モーション: オート回転の1周秒数はレートに同期(CFG.dialSpinSec・高=勢いよく)。オート中タップはばね回転で応える。reduced-motionで回転・点滅停止（機能維持）。

### §9 通知の視覚化 — 状態表示の規約（イベント通知でなく「見ればいつでも分かる」状態）
- **原則（Ric・Phase9）**: 「イベント＝見逃せる／状態＝見ればいつでも分かる」を分離。継続する事実は**恒久の状態表示**にし、トーストで一度だけ語らない。
- **コオロギ在庫切れ**: topbarの🦗カウンタ `#ui-crickets-top` に `.crk-empty`（在庫<1）＝`--red`＋`blink`の恒久明滅。reduced-motionで明滅停止（赤は残す）。=「今コオロギが無い」を常時提示。
- **中央の軽い通知**（進行マイルストーン・ボス出現/撃破）: `Render.showCenterNotice`。**トースト化させない**＝同時1件・短時間(`CFG.centerNoticeSec`)・待ち行列上限(`CFG.centerNoticeQueue`・超過は古いものを捨てる)・タップ不要・暗転なし。飼育槽中央 y≈150。
- **メニュー新着ドット**: 図鑑/研究/称号などの解放は、該当メニューボタンに新着ドット（見るまで消えない）で示す（黙って消さない）。
- **負傷/回復**: テキストでなく尾の自切/再生（§9.1）で見せる（`fable` 例外＝魂の状態表現。詳細は render の `tailRegen`）。
- 配置: 飼育槽右下（既定）。標的はクランク64px級・レート/トグルも44px級。

### 5.8 ヒーローオーバーレイ（ピークの器）
- 全画面 or 大面積。暗転(`--glass`)→主役登場→発光→情報ワイプ→退場。`--dur-cinematic`。
- スキップ可（タップで即終了）。reduced-motion時は簡易フェード。
- 使用先は§6の"大胆な瞬間"に限定。乱用しない。

### 5.10 HUDトランジション（節目に挟む器・C2改訂 2026-07-28）
ヒーローオーバーレイ（§5.8＝稀な瞬間・重い）とは別枠の、**頻発する節目に挟む軽い器**。

- **器はCSS、中身は全てCanvas 2D**。器は `.holo-stage`（`position:fixed / inset:0 / background:#04060a`）**ただ1つ**で、
  内部に全面 `<canvas>` を1枚だけ持つ。描画は `js/holo.js` に閉じる（CSSは可視制御のみ＝見た目の知識を2箇所に置かない）。
  **器のDOM生成・寸法は `Holo.mount(id)` が唯一の窓口**（呼び出し側は寸法を知らない）。
  差込先ごとに異なるのは **`z-index` だけ**: `#holo-travel`=210（惑星移動・頻発）／`#holo-opening`=500（起動ブリーフィング・初回のみ・ヒーロー340や報酬320より上＝排他）。
- **配色はHOLOパレット**（`CFG.holoPal`：虚空 `#04060a` / 琥珀 `#ffb547` / 深紅 `#d2384a` / 白青 `#dfe9ee`）。
  §3のUIトークンではなくこちらに従う（ホログラムHUDは世界の内側の機構であり、UIシェルではないため）。
  例外＝**惑星など対象そのものの色**は、その対象のパレット（`STAGES.sky`）を引用してよい。虚空の上で読める明度まで底上げする。
- **共通様式＝走査ビーム**：白青のビームが横断し、通過後にHUDの格子が起動して残る。ビームは必ず画面外へ抜けて減衰し、**残らない**。
- **尺は1.2秒以下**（`CFG.holoTravelMaxSec` でクランプ）。頻発するものは軽く（§6 濃さの原則）。
  **例外＝オープニング**（一生に1度＝`CFG.holoOpenMaxSec` 15秒でクランプ・実尺14.8秒）。濃さの原則どおり「稀なものは重く」。
- **スキップは即時・確認なし**（クリック/タップ/キー）。**reduced-motion時は動かさず最終画を静止表示**し、情報は残す（§4.4）。
- **終わったら完全に消える**（器ごと `display:none`。平常画面に残り香を持ち込まない）。
- 差込先は現在「**惑星移動**」と「**オープニング（起動ブリーフィング）**」の2つ。追加時は本節に差込先を追記してから実装する。
- **オープニングの導線（2026-08-01）**: 初回起動時のみ自動再生（`CFG.openingAutoPlay`）。再生済みフラグは
  セーブの `dial` 内に持ち、**既存プレイヤーには流さない**。再視聴は**設定モーダルの1行だけ**（押し売りしない・
  データ初期化の直前に置く＝目立たない位置）。スキップは他の器と同じく即時・確認なし。

---

## 6. 大胆な瞬間（ヒーロー演出カタログ）

**若年層向けの"沸き"はここへ集中投下する。** 平常面には持ち込まない。

### 濃さの原則（Phase 5追記・ディレクター指示）
- ヒーローは大胆でよいが、**1〜2秒で終わる／必ずスキップできる**。
- **頻発するものは軽く・稀なものは重く**（濃淡を意図的に）: 巣ノード解放=中庸（大げさすぎない・でも気づく）／ロケット・伝説誕生など"一生に数回"級=作り込む。
- 演出はオーバーレイ層に閉じ込め、終わったら完全に消える（平常画面に常時光る・常時動く残り香を持ち込まない）。

| 瞬間 | 演出方針 |
|---|---|
| ボス襲来・撃破 | 暗転→ボス降臨カットイン→撃破スロー→報酬噴出（V2演出をヒーロー品質へ底上げ） |
| 巣ノード解放 | 糸が走る→ノード発光→鉱石が中心へ流入。先行解放は特別演出 |
| 隕石を割る | 亀裂→閃光→レア個体お披露目（伝説はさらに大きく） |
| 惑星移動 | HUDトランジション（§5.10・走査ビーム＋行き先の惑星＋実データ・1.1秒以下・スキップ即時）。旧「宇宙船ワープ」は `CFG.holoTravelOn:false` の復帰先として残置 |
| ロケット発射 | 完成時のフルスクリーン打ち上げ＋Lore |
| 成長・進化・伝説誕生 | 光の柱＋称号スタンプ（レトロフォント差し色の見せ場） |
| レベル/ランクアップ | 数値ロールアップ＋リング演出 |

---

### 6.1 危機状態(Dread・Brushup V2 Phase 3追記)
- ボス級(tier/boss/elite)の戦闘中のみ `body.dread` が立ち、①飼育槽の赤ヴィネット(脈打つ) ②ヘッダー縁が`--boss-500`へ ③襲来瞬間のごく軽い震動、を与える。**戦闘終了で即・完全に平常へ**(残り香ゼロ)。
- 強度は `--dread`(0〜1)で一元制御。弱=0.5から開始しディレクター確認で調整。
- reduced-motion: 脈動・震動を止め、静的な赤ヴィネットのみ。
- **ボスHPバー(HUD)**: 戦闘中のみ飼育槽上部中央にDOMで表示(終了で即消える・常設しない)。構成=`[ポートレート(機器窓)][ボス名(DotGothic16)+ティアバッジ+ELITE][HPバー+等幅数値+残り時間]`。Enrage時はバーと名が赤く脈動(既存判定に描画を同期)。

## 7. アクセシビリティ（必須）
- コントラスト: 本文はAA（4.5:1）以上。土壌背景×`--paper-100`で担保。
- クリック標的: 小さすぎる操作対象を避け、実描画が小さくても判定を拡張する。
- ホバー/フォーカス可視化: マウスのホバー状態とキーボードのフォーカスリングを明確に出す。
- `prefers-reduced-motion`対応（§4.4）。
- 色のみに依存しない（§1.6）。
- 対象はPCブラウザ。スマホ/タッチ最適化（親指到達・セーフエリア等）は本プロジェクトの対象外。

---

## 8. 推奨ファイル構成（ui.js分割）

現状の巨大`ui.js`は、責務混在で一括置換事故の温床（V4.1で openHQ/openAllies 消失の実例あり）。**UI刷新を機に責務単位へ分割**する。分割は挙動を変えない純リファクタとして先行実施（Sprint Phase 0）。

推奨モジュール（実名は既存規約に合わせる）:
- `ui/tokens`（本ファイルのトークン定義の実体）
- `ui/components/*`（button / pill / card / modal / toast / tabbar / badge）
- `ui/screens/*`（main / nest / hq / planetMap / equipment / breeding / dex）
- `ui/hero/*`（boss / nestReveal / meteorite / warp / rocket）
- `ui/motion`（カウントアップ・stagger・パーティクルの共通ヘルパ）

原則: **1ファイル1責務。関数のエントリポイント（openXxx等）は各screenモジュールに集約し、跨いだ一括置換をしなくて済む構造**にする。

---

## 9. アイコン方針（絵文字全面禁止）

- **絵文字は一切使わない。** UI・ラベル・トースト・ヒーロー演出・資源表示・通知バッジを含め、本文中の装飾記号としての絵文字（ 等）を全廃する。
- すべてのアイコンは**自作のSVGまたはスプライト**で用意し、統一されたアイコン言語（線幅・角丸・塗り/線のルール）に従う。
- 資源・鉱石・惑星・ボス・味方・設備など、これまで絵文字で仮表現していた箇所は、対応するSVGアイコンへ差し替える。
- アイコンは`currentColor`または色トークンで着色し、テーマ変更（惑星アクセント等）に追従できるようにする。
- レトロの香りはドットフォント（§2）とドット風SVGで出す。絵文字には頼らない。

### 実装（SVG化スプリントで確定）
- スプライトは `js/ui/icons.js` の `ICONS`（24x24グリッド・線幅2・丸端・currentColor）。`Icon.svg(id)` でインライン`<use>`参照。
- CSSは `.icon`（1.1em角・ベースライン-0.18em）。色は親のcolor＝トークンに追従。
- 例外として残す純テキスト記号: `→ ← ⇔ ▸ ★ ☆ ✦ ×`（タイポグラフィであり絵文字ではない。等幅・単色でレンダリングされる）。


## §11 遺伝子ルーレット(V5・roulette.md準拠)

- 設置=左メニュー下部 `#roulette-card` > `#roulette-wrap` > `#roulette-canvas`(専用Canvas・魂のメインCanvasとは別)。
- 描画は表現層(js/ui/screens/roulette.js)がルール層(Roulette)のstateを毎フレーム描くだけ。数値/確率はルール層。
- **レインボー演出**: レインボー帯は七色の淡いグロー(時間で色相回転・当たり位置の明示)。着弾時 `BallEnteredRainbow` イベントで七色バースト+リング(~0.9s減衰)。新種誕生時は枠に `roul-pop` の一瞬の発光。
- モーション: `roul-pop` = ルーレット枠が0.4sで一度だけ発光する当たり演出(prefers-reduced-motionで停止)。
- 色は生命緑/七色HSL。reduced-motionでバースト等を抑制、結果(卵)は保証。

## §12 特性カード(V5 特性システム S3・trait_system.md §12)

個体の「特性(trait)」をコレクションカード的に見せる。**個体詳細を開いた時だけ表示**(フィールド常時表示は増やさない=§9「見せる」方針・平常画面を騒がしくしない)。

- **設置**: 個体詳細パネル(`js/ui/screens/main.js` renderDetail)。旧「★レア度＋モーフ名」行を撤廃し、その位置に特性カード列を置く(レア度の**表示のみ**撤廃=内部数値=経済係数は残置・trait_system 判断⑦(i))。
- **構造**: `.trait-cards`(横並び・折返し)> `.trait-card` × 最大3。1カード = `[特性色の左アクセントバー][SVGロゴ(特性色)][特性名][固定印(あれば)]`。特性を持たない個体は `.trait-empty`「特性なし」を控えめに。
- **色/ロゴ**: 各特性は `TRAITS[key]` に `color`(カード地の色調)・`rim`(明るい徴色=アイコン/アクセントに使う=暗背景で可読)・`icon`(ICONSのSVG id・§9絵文字禁止)を持つ。ミミカクシ=藍(`--tc`= rim `#7c93d4`)＋仮面ロゴ(`trait-mask`)。
- **トークン**: カード地=`--ink-700`/縁=`--ink-600`/左アクセントバー3px=特性色(`--tc` inline)/内側1pxハイライト＋`--elev-1`級の淡い影。文字=`--paper-100`・`font-size: calc(12px * var(--fs-scale,1))`。**Neo-Terrarium(落ち着いた土壌トーン)に収める=派手にしない**。コレクション感は「色アクセント＋ロゴ＋角丸カード」で出し、発光や強い彩度は使わない。
- **固定印(S5-b)**: 固定された特性(`Game.isFixed`)に小さな鍵アイコン(`lock`)。「必ず継がれる」が一目で分かる控えめな印。繁殖ピッカーにも同印を出す(S3③)。
- **レジェンダリー例外**: レジェンダリーは特性の対象外(§16)。特性カードを出さず、従来どおり `.legendary-tag`「レジェンダリー」の特別表示(琥珀/七色の格式)を維持。虹色発光(魂)が最上の個性。
- **モーション**: 出現は`--dur-base`のフェード＋僅かな上げ(任意)。**reduced-motionでアニメ無効=即時表示**(静的でも成立)。#spin-proof・魂の描画には一切干渉しない(DOMのみ)。

## §13 研究デスクパネル(R4改訂 2026-07-25: ロケット要素撤去・統一書式ほかUX改善は維持)

- **経緯**: 旧「建造計画」(みちのりトラッカー/建造投資青写真ブロック)はR4本部復元でロケット構想ごと撤廃(git記録)。「何を投じればよいか分からない」問題の解決=以下のUX改善はロケットと独立のため維持する。
- **設置**: 本部デスク群パネル(`js/ui/screens/hq.js` openLabDesks)。パネル名=「研究デスク」。構造: ①設備投資(統一書式素材列+不足で沈む投じる) ②変換(保有内蔵) ③惑星開発/侵食 ④研究リスト ⑤手持ち資源(降格)。機能ID(`#lab-invest`/`#lab-invest-btn`/`#cv-*`/`#hq-dev`/`#hq-invasion`/`#research-list`)は既存QA契約として不変。
- **素材表示の統一書式** `.mat`(パネル全域で唯一の書式): `[アイコン] 素材名 保有X / 必要Y`。アイコン単独の要求表示は全廃。充足=`.mat.ok`(琥珀点灯 `--plan-cur`=CFG.planCurColor)/不足=`.mat.ng`(沈み `--plan-dim`=CFG.planDimOpacity+「あと◯」だけ深紅 `--plan-lack`=CFG.planLackColor)。**色だけに頼らず明度と文言で二重化**。
- **変換** `.plan-cv`: 各ボタンに変換元の保有を内蔵(`.cv-have`)。保有<コスト=沈み+「あと◯」深紅。
- **研究リスト** `.plan-research`: 現行項目・順序維持。コストは統一書式。ロック行=錠前+**ホバー(title)で開放条件**(常時表示しない=気配の作法)。
- **手持ち資源** `.plan-pocket`: 俯瞰用に存続・一段沈める(opacity .55)。
- **CFG**(`CFG.plan*`): planCurColor/planLackColor/planLackWord/planDimOpacity(色はパネルルートのCSS変数へ)。
- **β遮蔽(P3-3・2026-08-11 Ric指示)**: `CFG.researchBetaHidden=true` の間、④研究リストのみ封鎖ブロック `.rsr-redacted` に置換(①②③⑤は不変=巻き添え禁止)。構成=見出し(錠前+等幅英字 `CFG.researchBetaEn`・REDACTEDのみ深紅)/マスクバー3本(縞ストライプ・文字情報ゼロ)/和文告知 `CFG.researchBetaJp`(「今後のアップデートで公開予定」=壊れているでなく「これから来る」)。語彙はC2遮蔽表示(REDACTED/UNRESOLVED)と統一・偽情報を書かない(holo.js冒頭の規約と同源)。研究名/コスト/購入ボタンをDOMに一切出さない(把握不能の保証=装置QA両面検査)。購入済み効果(`Game.researchBonus`)は表示と独立に継続。`false`で従来リスト復帰(β後の解禁)。


## §14 本部右メニュー(v5 HOLO COMMAND 操作系・2026-07-25)

- **目的**: パネル入り口を黒空間クリック(未点灯モジュール)に依存させない常設導線。**左メニュー=飼育槽の操作系/右メニュー=本部の操作系**の場所分離。「←飼育槽へ戻る」は左のまま(帰り道=左・本部操作=右)。
- **設置**: `#hqlab` 内にJSが動注入(`#hqlab-row`で#hqlab-wrapと横並び・飼育槽レイアウトへの影響ゼロ)。本部滞在中のみ表示(セクションのhidden連動)。
- **項目**(順序・ラベル=`CFG.hqMenuItems`): 研究デスク/錬成/標本棚/宇宙港/図鑑。既存openLabPanel系・openDexを呼ぶだけ=ロジック非接触。ホロモジュールの直接クリックも存続(二重導線)。全tierで全項目表示。
- **意匠**: v5 HUD言語=琥珀(#ffb547系alpha)・四隅ブラケット(borderの角)・等幅英字サブラベル・ホバーで点灯(bg/border/文字のalpha上げ)。PoCモジュールと喧嘩しない縦置きの控えめなタブ列。アイコン+短い和名+英字サブ。
- **縮退**: 幅900px未満=アイコンのみ(ラベル非表示・幅=`--hqmenu-w-narrow`)。幅=`CFG.hqMenuWidth`/`hqMenuWidthNarrow`をCSS変数へ。
- **アクセシビリティ**: button要素・title属性にフル名称。ホバー点灯はopacity/borderのみ(点滅なし=reduced-motion配慮不要の静的変化)。

## §15 繁殖ピッカーv2(R1・種×モーフタイル+A/Bスロット・2026-07-25承認=案B)

- **導線**: 左メニュー「繁殖」(設備の直下・ft-card意匠)+個体詳細の「繁殖相手を選ぶ」(=A事前選択で同モーダルへ)。旧・系統→個体二段階/相手行リストは完全置換(git記録+撤去前スクショ)。
- **タイル** `.sp-tile`: 種×モーフ単位(案B=所持モーフが見える・図鑑軸と一致)。個体ミニ姿(canvas)+種名+「モーフ × n匹/繁殖可 m」バッジ(CFG.breedCountBadge)。最小幅=CFG.breedTileMinW。A選択済=琥珀「A」バッジ(.picked-a)。**同種同モーフの繁殖可残who(A除く)が0=グレーアウト**(.grayed)。繁殖可0のタイルも表示(グレー・全体像を隠さない)。
- **A/Bスロット** `.sp-slot`: 姿(canvas)+名前+モーフ/Lv+特性チップ+固定印(S3③資産=breedTraitChips流用)+◀▶(同タイル内を**id昇順の安定順序**で巡回)。最小高=CFG.breedSlotMinH。
- **確定**: `Game.breed(aId,bId)`(唯一の実行経路=旧UIと同一関数・コスト/CD/確率/遺伝は不変)。モーダル下部のボタンは**この確定1つだけ**。
- **★自動選出の撤廃(Ric裁定 2026-07-29)**: **手動での掛け合わせこそがゲームUXの核**であり、自動選出と予約はその体験を痩せさせる。よって
  **クイック繁殖ボタン / 希少スコア式(CFG.breedScoreW・breedScoreUpMut) / 繁殖予約トグル(autoBreed)** を撤廃した。
  タイル選択時の自動充填と◀▶の巡回は**スコア順でなく id 昇順**=プレイヤーが順序を予測できる。
  **繁殖に自動化を再実装しない**(給餌の自動化がクランク経路のみに限定されているのと同じ規律)。維持するのは
  タイル / A/Bスロット / 特性チップ / 固定印 / ◀▶ / 同種残0のグレーアウト。
- **アクセシビリティ/規律**: チップ規律は§12準拠・行高/チップ領域はS3③のCSS変数を流用。モーダル開閉は既存挙動。
